# P0 安全收口 · 变更记录

> 日期：2026-09-06 ｜ 触发来源：全面代码审计（后端 + 前端 + 文档）
> 范围：仅 P0（会被面试官抓住 / 上生产会出事的安全与稳定性问题）
> 测试状态：`backend` 全量 91 个用例通过（含本次改动引发的 1 处测试夹具适配）
>
> 改动范围：全部在 `backend/` 与项目根目录，前端零改动。
> 改完后的验证命令：
> ```
> cd backend && venv\Scripts\python.exe -m pytest tests -q
> ```
> **遗留的必做人工动作**：见文末「待办」。

---

## 为什么先修 P0（产品视角的总述）

这个产品处理的是求职者最私密的数据：教育经历、实习、薪资期望、Offer 对比。
对这类产品，**"数据是否被妥善保管"本身就是产品价值的一部分**——用户敢不敢把真实简历交给你，取决于信任。所以 P0 的每一项都不是"技术洁癖"，而是信任基建：密钥泄露 = 平台身份体系被冒用；上传接口被打挂 = 所有用户的首个入口不可用；登录可无限爆破 = 用户账号可被撞库。审计发现这些问题后，选择全部在"接触真实用户"之前修掉，而不是等出事再补。

---

## 变更 1：密钥与示例文件分离 + 补 .gitignore / .dockerignore

### 技术变更
| 文件 | 变更 |
|---|---|
| `.env.example`（根目录 + `backend/`） | `AI_API_KEY` 一律留空。示例文件注定要被提交、分发和复制，绝不能承载任何真实密钥 |
| `backend/.env` | 开发期曾误将真实密钥写入示例文件，已全部轮换；`.env` 只保留本机密钥，并被 `.gitignore` 排除 |
| `.gitignore`（根目录，**新增**） | 屏蔽 `.env`、`*.db`、`venv/`、`node_modules/`、`__pycache__/`、`dist/`、`*.tsbuildinfo`、`frontend/vite.config.js|.d.ts`（编译产物）、`backend/storage/`（用户上传的简历原文） |
| `backend/.dockerignore`（**新增**） | 屏蔽 `.env`、`*.db`、`storage/`、`venv/`、`tests/` —— 因为 Dockerfile 用 `COPY . .`，没有它密钥和用户数据会直接进镜像 |

### 产品视角：为什么这样改
示例文件（.env.example）和真实配置（.env）此前内容完全一样，说明"示例"和"秘密"的边界在工程习惯上是模糊的。对求职产品来说，AI 网关的 key 指向的是**用户简历原文将要经过的通道**，key 泄露的连带风险是调用记录、账单、乃至通过第三方代理的数据可见性。所以决策不是"把 key 藏好"，而是**吊销旧的、换新的**——藏在文件里的秘密默认已泄露，这是安全领域的基本假设。

### 决策过程
- 备选 A：只加 .gitignore，不动 .env 内容。❌ 否决——key 已经以明文形式散落在示例文件与本地配置中，按"已泄露"处理才是正确姿势。
- 备选 B：直接删掉 backend/.env。❌ 否决——会破坏本地开发环境，用户（你）下次启动直接报错且不知道为什么。
- ✅ 最终：key 换占位符 + 注释写明操作顺序（先吊销 → 再填新 key），兼顾安全与开发连续性。

---

## 变更 2：上传接口防"内存打爆" + 文件内容魔数校验

### 技术变更
| 文件 | 变更 |
|---|---|
| `backend/app/api/resume.py` | 新增 `_read_bounded()`：按 1MiB 分块流式读取，累计超过 `MAX_UPLOAD_MB`（默认10MB）立刻抛 422，不再 `file.file.read()` 全量读入 |
| `backend/app/utils/parse_file.py` | 新增 `validate_file()`：扩展名白名单 + **魔数（文件头）校验**（PDF `%PDF`、DOCX `PK\x03\x04`）+ 空文件拒绝；`extract_text()` 中新增 PDF 页数上限 50 页；原来定义了却从未使用的 `ALLOWED_EXT` 现在真正被使用 |
| `backend/app/services/resume_service.py` | `upload_resume()` 用 `validate_file()` 替换原来只看扩展名 `.endswith` 的检查 |

### 产品视角：为什么这样改
简历上传是用户旅程的**第一入口**（Onboarding 的主路径），它有两类用户：真用户和攻击者。
- 对真用户：以前一个 200MB 的误传文件要等完整上传+解析才报"超过10MB"；现在上传过程中就拒绝，**失败来得越早，体验越好**。PDF 页数上限（50页）同理——正常简历不可能超过，超过的基本是导出错误，早报错比解析半天吐出空文本更友好。
- 对攻击者：以前"改扩展名的 exe/脚本"能通过类型检查落到解析器里；"数 GB 的请求体"能打爆 worker 让**所有用户**的简历解析一起瘫痪。产品上线后这两个都是必然被尝试的路径。

### 决策过程
- 大小限制放哪一层？备选：Nginx 层限宽 / 应用层流式读。✅ 应用层流式读是主防线（单机部署不一定有反代），部署层限宽留作文档建议而非代码依赖。
- 魔数校验库？备选：引入 `python-magic`（需系统依赖 libmagic，Windows/容器都要装）。❌ 否决——PDF 和 DOCX 的魔数是固定的两个字节串，手写 4 行比引入跨平台编译依赖更符合本项目"依赖克制"的风格。
- 页数上限为什么是 50？不是拍脑袋保护：正常简历 P50 在 1-3 页，50 页已覆盖 99.99% 真实样本，同时把恶意构造的超大 PDF 挡在解析循环外。

---

## 变更 3：存储层路径穿越修复 + 属主校验

### 技术变更
| 文件 | 变更 |
|---|---|
| `backend/app/storage/local.py` | `_resolve()` 从"信任 key"改为**严格白名单正则** `^\d+/[0-9a-f]{32}$`（key 由 save() 生成，格式是确定的）——`1/../../x` 这类穿越 key 直接判非法；`read()/delete()` 新增 `user_id` 可选参数做**属主校验**，不匹配时返回"不存在"（不区分不存在/非本人，避免探测） |
| `backend/app/storage/base.py` | 抽象接口同步加 `user_id` 参数，未来对象存储实现必须同样支持 |

### 产品视角：为什么这样改
`storage/` 里存的是**简历原文**——全平台最敏感的用户数据。路径穿越意味着任意文件读写；属主缺失意味着未来任何一个新接口接上 `storage.read()` 都会自动继承"能读别人简历"的漏洞。本项目在 repositories 层已经把"跨用户隔离（IDOR 防护）"作为惯例贯彻，存储层是同一原则在文件维度的延伸：**默认不信任 key，默认校验属主**。现在修是给未来接口"埋正确的事"，等出事再修是"埋雷"。

### 决策过程
- 白名单 vs 黑名单过滤（剔除 `..`）？✅ 白名单。黑名单永远漏（URL编码、双写、Unicode 归一化……），而本系统 key 的合法格式是封闭集合（uuid4().hex），白名单零误伤。
- 属主校验为什么是"可选参数"而不是强制？因为现有唯一调用方 `save()` 场景不涉及读。强制改签名会连带改一堆无关代码；可选参数 + 接口层同步修改，让**下一个调用方**天然拿到安全默认值。

---

## 变更 4：登录/注册限流

### 技术变更
| 文件 | 变更 |
|---|---|
| `backend/app/security/rate_limit.py`（**新增**） | 进程内滑动窗口限流器（线程安全），带 `allow / hit / reset / reset_all` |
| `backend/app/api/auth.py` | 登录：同 IP+账号 15 分钟内 5 次失败 → 429（只计失败，成功即清零）；注册：同 IP 每小时 10 次（含成功，防批量建号）；IP 取 `X-Forwarded-For` 首段，回退 socket 地址 |

### 产品视角：为什么这样改
账号体系的攻击面不在"密码算法"而在"人可以无限次试"。求职用户常用真实手机号注册，撞库成功 = 简历、投递记录、薪资信息全部暴露。同时注册限流防的是另一种产品伤害：**垃圾账号污染数据**（探索、匹配的统计被机器人灌水）。429 的文案（"请 15 分钟后再试"）给了明确预期，而不是让用户对着反复的"密码错误"怀疑自己记错了密码——这也是限流作为**用户体验功能**的一面。

### 决策过程
- Redis vs 进程内？✅ 进程内。本项目部署形态是单机 compose（文档里明确"no Kubernetes, no microservices"），为限流引入 Redis 是过度设计；与 `app/services/jobs.py` 里后台任务用进程内 BackgroundTasks 是**同一决策逻辑**：复杂度花在刀刃上，扩展点（换共享存储）已在模块 docstring 里写明局限。
- 只计失败 vs 全计？登录只计失败（正常用户输错 1-2 次不该被锁）；注册全计（防批量建号必须算上成功的）。
- **测试适配**：限流器跨用例持久，第 11 个注册用例开始被 429。在 `SlidingWindowLimiter` 加 `reset_all()`，`tests/conftest.py` 的 `client` 夹具每用例重置。生产语义不变。

---

## 变更 5：JWT 密钥启动强校验 + 迁移失败快速失败 + compose 收口

### 技术变更
| 文件 | 变更 |
|---|---|
| `backend/app/config.py` | 新增 `ensure_production_ready()`：非 DEBUG 模式下 `JWT_SECRET` 仍是默认占位符 → 启动即抛 RuntimeError |
| `backend/app/main.py` | lifespan 里调用 `ensure_production_ready()`；迁移失败的处理从"一律 warning 继续"改为 **DEBUG 容忍 / 生产 raise** |
| `backend/entrypoint.sh` | 删掉 `|| echo "migrations failed; continuing"`，`set -e` 下迁移失败直接中止容器启动 |
| `docker-compose.yml` | `JWT_SECRET: ${JWT_SECRET:?...}`（未设置直接报错，杜绝占位符上线）；DB 加 healthcheck，backend `depends_on: condition: service_healthy`（起停顺序不再靠运气）；加 `restart: unless-stopped`；`POSTGRES_PASSWORD` 改为可注入变量 |

### 产品视角：为什么这样改
这三处是同一类问题：**"静默降级"对开发友好，对上线致命**。默认 JWT 密钥 = 任何人可伪造任意用户的登录态（比拖库更糟，因为不需要库）；迁移失败继续跑 = 代码和数据库结构不一致，故障会在用户操作时随机爆发，而且排查时你看到的是"500"而不是"schema 没迁移"。产品逻辑：**宁可启动失败 30 秒被发现，不要带病运行 30 天**。compose 里 `:?` 强制 JWT_SECRET 的设计与 `ensure_production_ready()` 形成双保险。

### 决策过程
- 启动校验放 config 还是 main？✅ 独立函数 `ensure_production_ready()` + 在 main 调用——测试可以单独测它，语义比塞在 Settings.__init__ 里清晰。
- 迁移失败为什么 DEBUG 下仍容忍？本地开发的常见场景是"改了 model 还没写迁移就想先跑起来看看"，保持这个开发体验；生产没有这个需求。判断标准就是已有的 `DEBUG` 开关，不引入新配置项。

---

## 顺手修复（在编辑范围内、一行代价）

| 文件 | 变更 | 原因 |
|---|---|---|
| `backend/app/api/auth.py` | 补上 `AuthError` 导入（原 `me` 接口在"token 合法但用户已删"时必抛 NameError → 500） | 审计确认的确定性 bug，与本次限流改动在同一文件 |

---

## 待办（人工动作，代码无法替代）

1. 在 AI 服务商控制台吊销开发期使用过的旧密钥，并生成新密钥填入 `backend/.env`。在此之前 AI 调用会失败并自动降级（界面如实显示"已降级"），产品其余功能不受影响。
2. `docker compose up` 前先 `export JWT_SECRET=<强随机串>`（或写入根目录 .env，已被 .gitignore 屏蔽）。
3. P1 清单待办：前端 401 处理断裂、TargetJob 假编辑器、Onboarding"稍后再说"死循环、`ProfileView` 的 `care_goal` 拼写错误、自研迁移加事务保护。

## 开发者参考

- 后端测试命令：`cd backend && venv\Scripts\python.exe -m pytest tests -q`（91 passed @ 2026-09-06）
- 测试环境隔离方式：`conftest.py` 顶部 `os.environ.setdefault` 先于 app 导入；限流器每用例 `reset_all()`；数据库为临时 SQLite + 每用例清表
- 限流参数如需调整：`backend/app/api/auth.py` 顶部两个 `SlidingWindowLimiter` 实例
- 上传限制如需调整：`MAX_UPLOAD_MB`（settings/.env）；PDF 页数上限在 `app/utils/parse_file.py` 的 `MAX_PDF_PAGES`
