# P2 工程化收口 · 变更记录

> 日期：2026-09-06 ｜ 前置：P0 安全收口、P1 核心旅程修复
> 范围：P2 —— 工程化补课（lint/test/CI）+ 错误处理盲区 + 轮询收敛 + 后端补测试
> 验证状态：后端 **111 passed**（新增 17）；前端四道门全绿 —— **lint 0 问题 / typecheck / vitest 7 passed / build 成功**
>
> **维护者须知**：前端现在有完整的质量门，改任何前端代码后必须过：
> ```
> cd frontend && npm run lint && npm run test && npm run build
> ```
> 后端：
> ```
> cd backend && venv\Scripts\python.exe -m pytest tests -q   # 111 passed
> ```
> 项目尚无 git 仓库，`.github/workflows/ci.yml` 已就位，`git init` + 推到 GitHub 后 CI 自动生效。

---

## 为什么做 P2（产品视角的总述）

P0/P1 修的都是"已经发生的问题"，P2 做的是"**让下一类问题不再发生**"。这轮之前的状态是：
代码质量靠作者自觉（事实上很自觉——TypeScript strict 全开、零 any、测试覆盖认真），但**没有任何机器强制**。lint 配置不存在但代码里写着 eslint-disable 注释、typecheck 脚本是坏的、前端一行测试都没有。对一个要作为代表作展示、还要持续迭代的产品，工程化是"迭代速度"本身：没有 CI，每次改动的回归风险都靠手测；没有 lint，新代码的质量没有下限。P2 的产品逻辑一句话：**让"保持高质量"从意志力问题变成流程问题**。

---

## 变更 1：后端补测试（storage / 限流 / 文件校验）

### 技术变更
| 文件 | 内容 |
|---|---|
| `backend/tests/test_storage.py`（新增，11 用例） | LocalStorage 往返读写删、**属主校验**（他人读取返回"不存在"）、**7 种非法 key 全部拒绝**（`../` 穿越、编码穿越、非 hex、多段等）、validate_file 魔数校验（真 PDF/DOCX 通过、伪装文件拒绝、空文件拒绝、非法扩展名拒绝） |
| `backend/tests/test_rate_limit.py`（新增，6 用例） | SlidingWindowLimiter：限额内放行/超限拒绝、窗口滑过自动恢复、key 之间隔离、reset/reset_all。用可控假时钟替代真实 sleep，CI 上确定性通过 |

### 产品视角
P0 给存储层加了路径穿越防护和属主校验，但当时没来得及配测试——**安全修复没有测试等于没修完**：下一个改动可能无意中绕过它，而没有人会知道。这轮补上后，"他人不能读你的简历"和"上传的文件必须内容真实"这两个产品承诺，从代码约定升级为每次 CI 都验证的事实。限流器同理：登录保护的可靠性应该被测试锁定，而不是靠"看起来对"。

### 决策过程
- 限流测试用假时钟而非 sleep(窗口时长)。✅ 假时钟（monkeypatch `time.monotonic`）——真实 sleep 会让窗口过期测试要么慢（30s+）要么 flaky，都不可接受。

---

## 变更 2：公共轮询 hook（消灭 4 处复制 + 补上限）

### 技术变更
| 文件 | 变更 |
|---|---|
| `frontend/src/hooks/usePolling.ts`（新增） | 通用 `usePolling`（fetch/decide/onDone/onGiveUp + maxAttempts，网络错误也计数，卸载自动停止）+ 业务封装 `useResumeParsePolling`（**90s 上限**） |
| `frontend/src/pages/Onboarding/ResumeInline.tsx`、`ResumeFlow.tsx`、`ScratchInput.tsx`、`pages/Profile/index.tsx` | 删除各自手写的 setTimeout 轮询，换统一 hook；Profile 的画像生成轮询上限 120s |

### 产品视角
修复前四处轮询的共同问题：**网络错误也无限重试、永远不放弃**。用户在弱网下打开解析页，前端会以 1 秒/次的频率永远打下去——耗用户电量、耗后端资源，而用户只看到"AI 正在读取你的简历…"直到天荒地老。修复后的行为：解析最长等 90 秒，超时给明确文案（"解析等待超时，请稍后重试或改用其他录入方式"）并引导用户换录入方式——**坏消息 + 出路**，好过无声悬挂。同时四处合一后，将来把轮询升级为 SSE 推送只需改一个文件。

### 决策过程
- 上限为什么 90s？真实 AI 解析通常 10-40s（P0 验证报告有记录），90s ≈ 3 倍余量；画像生成链路更长（F2），给 120s。
- 通用 hook vs 各写各的？✅ 一个通用 usePolling + 一个业务语义封装（useResumeParsePolling）。通用层管机制（计时/上限/取消），业务层管语义（什么算完成、失败文案）——两层各自可测（见变更 4）。

---

## 变更 3：Offer / Explore 错误处理盲区（11 处）

### 技术变更
| 文件 | 变更 |
|---|---|
| `frontend/src/pages/Offer/index.tsx` | 删除 Offer（加确认 + catch + toast）、比较按钮（busy + catch）、OfferForm.submit（busy + 表单内错误提示）、标记接受（catch）、AI 评估 / 综合评分（busy + 行内错误）、薪资计算（busy + 行内错误，首屏计算不再 unhandled rejection） |
| `frontend/src/pages/Explore/index.tsx` | 排除/候选/对比/设为目标方向（catch + setError + actionBusy 防连点）、行业/岗位详情（`.catch(() => {})` 改为真实错误提示） |

### 产品视角
这两个页面的共同模式：**操作失败了，但界面毫无反应**。用户点"删除"没有反应会再点三次（如果是网络抖动，可能触发三次删除请求）；点"AI 评估"没反应会认为功能坏了。修复后统一遵循"**每个动作要么成功反馈、要么失败反馈、要么显示进行中**"的三态原则——这也是全站其他页面已有的约定（六个工作区首屏早就有 LoadingState/ErrorState），这次是把 Offer/Explore 的局部操作拉齐到同一水准。防重复提交（busy 门闩）顺带消除了一个隐性数据风险：连续点保存可能创建重复 Offer。

### 决策过程
- 删除确认用什么？项目里已有自研 Modal，但 Tracking/TargetJob 用的是 window.confirm（轻微问题 #17）。本轮 Offer 跟随 window.confirm 保持现状一致，统一替换为 Modal 组件留给后续 UI 打磨（避免一次改动同时引入风格迁移）。

---

## 变更 4：前端质量门落地（ESLint + Prettier + Vitest）

### 技术变更
| 文件 | 变更 |
|---|---|
| `package.json` | 新增 devDependencies（eslint 9 + typescript-eslint 8 + react-hooks/react-refresh 插件 + prettier 3 + vitest 2 + jsdom + testing-library）；scripts 新增 `lint`（**--max-warnings 0**，警告也是失败）、`test`、`format`；**修复 `typecheck`**（`tsc -b --noEmit` 的 --noEmit 与引用项目 emit 冲突 → `tsc -b`，产物文件已被 .gitignore 覆盖） |
| `eslint.config.js`（新增） | Flat config；规则基调与既有代码风格对齐：`no-explicit-any` 禁止、未使用变量须 `_` 前缀、react-hooks/exhaustive-deps 开启；`react-refresh/only-export-components` 关闭（Provider+hook 同文件导出是本项目的刻意约定） |
| `.prettierrc`（新增） | printWidth 110 / 分号 / 双引号。**未做全量 reformat**（避免淹没本次变更的可读性），`npm run format` 可随时执行 |
| `src/test/setup.ts`、`vitest.config.ts`（新增） | jsdom 环境 + jest-dom |
| `src/test/client.test.ts`（新增，3 用例） | **锁定 P1 的 401 修复**：带 token 收到 401 必须清 token 且广播 `cdp:auth-expired`；登录失败（无 token 401）不广播；其他状态码不动 token |
| `src/test/usePolling.test.tsx`（新增，4 用例） | 轮询完成/超时放弃/give-up 立即停/手动 stop 生效 |

### 产品视角
代码里原本就有 4 处 `eslint-disable` 注释——说明作者**期望有 lint 但从未配置**。P2 把期望变成现实，并且 lint 第一次运行就抓到了一个真实 bug（Modal.tsx 的 `title || true` 恒真表达式——一个小写法瑕疵，正是"没有机器检查时人眼会漏掉的东西"）。测试部分刻意先锁定**这次迭代修过的两个关键行为**（401 广播、轮询上限），而不是追求数字：**回归测试的价值在于钉住已经付出代价修好的东西**。

### 决策过程
- lint 门槛 `--max-warnings 0`？✅ 是。警告会无限繁殖（"反正只是 warning"），项目现有代码量下清零成本很低（首轮 10 个问题，逐个处理后归零），起步即严格是最便宜的时点。
- 既有 exhaustive-deps 警告怎么办？逐个判断：能用稳定依赖修复的直接修（TargetJob 的 decideStage、Onboarding 的 navigate）；**刻意 mount-only** 的用 disable 注释 + 原因说明（Offer 首屏自动开抽屉、SalaryDrawer 首算）。
- `react-refresh` 规则关闭是否妥协？是刻意的：Context 文件同时导出 Provider 和 useXxx hook 是标准 React 模式，为 HMR 优化拆文件是收益极低的折腾。

### 首轮 lint 发现并修复的问题
1. `Modal.tsx:40` `title || true` 恒真（error，已简化为始终渲染头部）
2. `usePolling` 依赖数组写法（已用解构修复）
3. Offer 两处 mount-only effect 缺显式声明（已加注释说明）
4. 测试文件缺 `vi` 导入（tsc 抓到）

---

## 变更 5：GitHub Actions CI

### 技术变更
| 文件 | 变更 |
|---|---|
| `.github/workflows/ci.yml`（新增） | 两个 job：backend（py3.13 + pip cache + pytest）／frontend（node20 + npm ci + lint + test + build）。本地有的质量门，CI 同样存在 |

### 产品视角
CI 是前面所有工程化投入的**固化**：storage 安全测试、限流测试、401 回归测试、lint 零警告——只有进了 CI 才能保证"不是只在作者机器上成立"。项目还没建 git 仓库，工作流已就位，`git init` + push 后即生效。对代表作的意义：面试官打开 GitHub 首页能看到绿色勾和 111 个后端测试 + 7 个前端测试的存在，这本身就是信号。

---

## 已知遗留（P3 候选）

1. **npm 依赖 7 个漏洞告警**（5 moderate / 1 high / 1 critical，来自 vite 5 / jsdom 等 dev 链）。未执行 `npm audit fix --force`（可能破坏 vite 版本）；建议单独开一轮：升级评估 + `npm audit` 逐条判断。
2. **Prettier 全量 reformat 未执行**（避免淹没本次变更）；`npm run format` 已就绪，建议在下个功能分支前单独跑一次。
3. JWT refresh token / 过期感知（后端 + 前端联动，独立专项）。
4. Tracking/TargetJob 的 window.confirm 统一替换为自研 Modal。
5. 简历解析轮询升级为 SSE 推送（usePolling 已把改动面收敛到一个文件）。
6. 死代码清理：`ResumeFlow.tsx`/`ScratchInput.tsx`/`ErrorState.tsx` 未被引用（本轮已同步维护它们，但应决定去留）。

## 给下一个 agent 的上下文

- 前端质量门命令与结果基线：`lint`（0/0）、`test`（7 passed）、`build`（tsc+vite）。**任何前端改动必须全绿**；CI 已配置（.github/workflows/ci.yml），推 GitHub 即生效。
- 后端基线：111 passed。新增测试文件：test_storage.py / test_rate_limit.py。
- 轮询：新代码一律用 `hooks/usePolling.ts` 的 usePolling / useResumeParsePolling，不要手写 setTimeout 轮询。
- lint 约定：`--max-warnings 0`；mount-only effect 必须留 disable 注释 + 原因；Provider+hook 同文件是约定（react-refresh 规则已关）。
- vitest 测试放在 `src/test/`，显式 import { describe, it, expect, vi } from "vitest"（tsc 需要显式导入）。
