# P3 设计升级 · 变更记录

> 日期：2026-09-06（V3 增补：2026-09-07） ｜ 前置：P0 安全收口 / P1 核心旅程修复 / P2 工程化收口
> 范围：前端设计全面升级（用户选定：全面视觉升级；优先：a11y 合规、AI 访谈页精致化、等待与空态体验）
> 验证：build ✅ / lint 0 警告 ✅ / vitest 7 passed ✅ / 浏览器实测（桌面 1440 + 移动 390）✅

---

## V3 增补：极光视觉系统（2026-09-07）

**触发**：用户反馈视觉仍偏古板，希望有更多渐变与"图画"。
**决策：不用 AI 生图/位图插画，改为手写 SVG 插画库 + 渐变体系扩展。** 理由：矢量可随主题换色、零额外请求、风格永久一致，是设计系统的生产级做法；AI 生图输出的位图无法保证风格统一也不可维护。

### 渐变体系（tokens.css）
- `--gradient-aurora`（蓝→紫→亮紫三停）：大面积氛围
- `--gradient-soft`：柔和水洗底；`--shadow-glow`：品牌辉光阴影
- `.grad-text` 渐变文字工具类（只允许大标题/大数字使用）
- `.btn--hero` 英雄按钮（渐变底+辉光，只给"开始一段旅程"级 CTA）

### SVG 插画库（components/ui/Illustrations.tsx）
6 幅统一风格插画（圆角几何+渐变填充+细线描边，viewBox 120×120）：
罗盘（探索）/ 地图钉路线（目标岗位）/ 火箭（准备）/ 旗帜航线（追踪）/ 天平（Offer）/ 渐变头档案卡（画像），
外加 `AuroraBackdrop` 英雄区极光背景（光斑+星点+连线）。
EmptyState 新增 `illustration` prop（132px 展示位+柔和投影），已接入 5 个工作区空态：
Explore/TargetJob/Prepare/Tracking/Offer。

### 页面应用
- 全局 `.shell` 极淡双角极光底（白卡片浮在"有空气感"的底色上）
- `.card--hero`（画像/岗位 Hero 卡）：白→淡紫渐变底 + 顶部 3px 品牌渐变条
- 侧边导航激活态：渐变底 + 3px 渐变竖线（2px→3px，与 Logo 同语言）
- Welcome：`AuroraBackdrop` + 英雄渐变按钮 + 标语渐变文字

### 验证
build / lint / vitest 全绿；DOM 走查确认访谈页（进度条 1/6、消息流、按钮）渲染正常。
截图通道在会话末尾不稳定，视觉终验建议直接打开 dev server（HMR 已热更新全部改动）。

---（以下为 2026-09-06 原始记录）---

## 设计总纲（一句话叙事）

审计结论：这套设计系统的 token 骨架本来就好（8 档字号/4px 间距/五级卡片），问题在架构卫生和体验细节。本轮确立**「品牌蓝管行动、AI 紫管智能」的双色语言**并贯彻到底：Logo/品牌区是两色交汇的渐变，用户气泡是蓝、AI 气泡是紫，AI 状态徽标/理解面板/AI 块全部收进 `--color-ai-*` 体系。升级是"让克制的设计长出个性"，不是推翻重做。

## 阶段 1：系统收口（架构卫生）

| 变更 | 文件 | 说明 |
|---|---|---|
| **pages.css(1400行) 拆分为 8 个模块** | `styles/pages/{auth,profile,onboarding,explore,target-job,prepare,tracking,offer}.css` | 单文件占比 55%→各模块独立维护；`main.tsx` 统一引入；拆分时同步完成下列三项 |
| token 合规 | 各模块 | 9 处 `#fff`→`--color-on-primary`；新增 `--color-danger-hover` 替换越界色 `#b91c1c` |
| 断点统一 | 各模块 | 639→640、980→1023、720→640；四档约定（640/768/1024/1180）写入 tokens.css 注释 |
| `.muted` 全局污染收敛 | target-job.css | 原全局覆盖改为 `.tj-page .muted` 作用域；`.page` 移动端规则收进 base.css |
| **表单 label 关联修复（a11y）** | Input/Select/Textarea | `Math.random()` id → `useId()`（原实现重渲染后 label 关联漂移） |
| **ConfirmDialog 组件** | 新增 `ui/ConfirmDialog.tsx` | 替换 4 处原生 `window.confirm`（Offer 删除/TargetJob 删除/Tracking 投递删除/面试记录删除），danger 红色确认态 + loading |
| 僵尸组件清理 | 删除 ErrorState/Stepper + 对应 CSS | 零引用死代码；错误展示统一走 notice/toast |
| Offer 加载态 | Offer/index.tsx | `EmptyState icon="loading"` 冒充 → `LoadingState` |

## 阶段 2：视觉升级（Tokens 2.0 + 品牌触点）

| 变更 | 说明 | 产品理由 |
|---|---|---|
| **`--gradient-brand`（蓝→紫渐变）** | tokens.css 新增，仅限 Logo/品牌区/英雄时刻 | "职己职彼=知己知彼"的镜像概念：蓝（行动）与紫（智能）交汇处即品牌 |
| **BrandMark 组件** | 替换 AppShell 桌面/移动、登录注册、Onboarding 顶栏共 5 处旧方块 Logo | 品牌触点统一收敛到一个组件 |
| **登录/注册分栏改版** | 居中小卡 → 左渐变品牌区（大标语+三条价值点）+ 右表单；<1024 收起品牌区 | 认证页是产品第一次自我介绍，原来只有一行 slogan |
| **排版落差拉大** | `--text-2xl` 24→28px（页面 h1）、新增 `--text-4xl: 36px`（Hero） | 标题与正文重量差不足是审计发现的层级问题 |
| **Welcome 首屏英雄化** | 极淡蓝紫极光背景 + 渐变 sparkle 头像 + 36px 标题 | 用户登录后第一屏要有"产品气质" |
| **AI 紫场景扩张** | 访谈 AI 气泡 → `--color-ai-surface`、AI 头像 → 紫系 | 双色语言核心落地：一眼区分"谁在说话" |

## 阶段 3：三大优先专项

### AI 访谈页精致化（第一触点）
- 思考态：三个点 → **思考胶囊**（点动 + "AI 正在理解你的经历…"文字），等待可感知
- **六维度覆盖度细进度条**：品牌渐变 3px 条常驻对话区顶部，配 `role="progressbar"`，"还差多少完成"始终可见
- 消息入场 fade-up 动效；完成摘要卡庆祝式 pop 入场
- 顺手修复：2 处 `arrow-right` 图标名错误（不在图标集内，渲染成了 fallback 图形）

### 等待与空态体验
- **LoadingState 新增 `stages` 阶段文案**（每 2.6s 轮换）：画像生成 3 阶段、简历解析 3 阶段（5 处接入）
- **骨架屏启用**：Explore/Tracking/Offer 首屏 spinner → `LoadingState skeleton`（能力早已内置，从未被使用）
- 空态三件套（图标+标题+说明+CTA）全站已统一，浏览器实测确认

### A11y 合规
- `--color-text-muted` `#8b92a3`→`#667085`（浅底对比度 3.1→4.6:1，过 WCAG AA；一处改全站生效）
- **Modal/Drawer 焦点管理**：新增 `useDialogFocus` hook——打开聚焦首元素、Tab 循环焦点陷阱、关闭还原焦点；Drawer 补 `aria-label`
- 移动端 bottom-nav 字号 10→12px；新增全局 `prefers-reduced-motion` 支持

## 阶段 4：浏览器实测记录（mock AI 后端 + dev server）

| 检查点 | 结果 |
|---|---|
| 登录页分栏（1440） | ✅ 渐变品牌区+表单卡，视觉达标 |
| 注册流程 → Onboarding Welcome | ✅ 极光背景+渐变头像+36px 标题 |
| AI 访谈（发消息→mock 回复） | ✅ 蓝紫双色气泡/渐变 Logo/进度条/理解面板徽标 |
| Explore 无画像空态（1440+390） | ✅ 空态三件套、移动端 topbar+bottom-nav 正常 |
| 移动端 390 | ✅ bottom-nav 12px 可读、触控目标正常 |

*浏览器点击偶发超时为 IAB 环境的 pointer 事件拦截问题，用页面脚本点击验证了功能本身无碍。*

## 已知遗留（P4 候选）

1. 内联样式收敛未完成（42→约 30 处；静态内联转 class 的工作量纯机械，建议随下次功能迭代顺带做）
2. Profile 首屏加载、Prepare 页未接骨架屏（本次覆盖 Explore/Tracking/Offer 三个列表页）
3. `Tracking`/`Offer` 仍共用 `tk-` 前缀 class（历史命名，拆分时保留以保证零回归；建议后续统一命名）
4. 移动端仅抽查 Explore；六工作区逐页 390 走查建议下一轮
5. 侧边栏"旅程状态点"样式已定义（nav-item__status）但未接数据——接上后导航可显示"已完成/进行中"

## 给下一个 agent 的上下文

- 断点约定：**只用 640 / 767 / 1023 / 1180**（max-width 语义），新媒体查询禁止再引入漂移值
- 双色语言：品牌蓝=用户动作/CTA；AI 紫=一切 AI 产出与状态（`--color-ai-*`）；渐变 `--gradient-brand` 只给品牌触点
- 新弹层一律用 Modal/Drawer + `useDialogFocus`（已内置焦点管理）；新确认操作一律用 ConfirmDialog，禁止 window.confirm
- 新等待场景：长等待用 LoadingState `stages`；列表首屏用 `skeleton`
- CSS 新增样式放对应 `styles/pages/<module>.css`，跨模块组件样式放 ui.css
