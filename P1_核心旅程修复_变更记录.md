# P1 核心旅程修复 · 变更记录

> 日期：2026-09-06 ｜ 前置：P0 安全收口（见 `P0_安全收口_变更记录.md`）
> 范围：P1 —— 直接伤害"用户核心旅程"的功能性 bug + 后端迁移可靠性
> 验证状态：后端 **94 passed**（新增 3 个用例）；前端 `npm run build`（含 tsc 编译）**通过**
>
> **维护者须知**：本次改动横跨前后端。后端新增了一个端点、迁移 runner 契约变更为
> "传入 Connection、单事务"；前端修了 4 个页面级 bug。测试命令：
> ```
> cd backend  && venv\Scripts\python.exe -m pytest tests -q      # 94 passed
> cd frontend && npm run build                                    # tsc + vite 全绿
> ```
> 已知预存在问题（非本次引入）：`npm run typecheck` 脚本因 `tsc -b --noEmit`
> 与 tsconfig.node.json 的 emit 设置冲突而报 TS6310，build 不受影响，待 P2 修脚本。

---

## 为什么先修这些（产品视角的总述）

P0 解决的是"用户敢不敢信这个平台"，P1 解决的是"**产品对用户的承诺是否兑现**"。
这轮修的每一个 bug 都有个共同模式：**界面展示了一个承诺，但点击/操作后承诺落空**——
显示可编辑但改不动（假编辑器）、显示可切换但切不过去（岗位切换）、显示"已关联"但没关联
（方向横幅）、说"稍后再说"却把用户弹回原地（死循环）。对一个以"AI 草稿 → 人工核对 → 确认"
为核心交互理念的产品，这类"假控件"是最伤信任的缺陷：用户会以为是自己不会用，而不会想到是产品坏了。所以 P1 的原则是：**每一处用户可见的承诺，背后必须有真实生效的链路**。

---

## 变更 1：登录态失效处理（401 断裂）

### 技术变更
| 文件 | 变更 |
|---|---|
| `frontend/src/api/client.ts` | 401 且请求确实携带 token 时：清 localStorage + `window.dispatchEvent(new CustomEvent("cdp:auth-expired"))` 广播会话失效 |
| `frontend/src/context/AuthContext.tsx` | 监听 `cdp:auth-expired` 事件，同步清空 React 内的 token/userId/email 状态（RequireAuth 因此能把用户送回登录页）；`login/register` 改为"me() 确认成功后才置登录态，失败回滚 localStorage" |

### 产品视角
token 过期是**必然发生**的事（7 天有效期），不是异常。修复前用户的体验是：所有请求反复报错，
页面没有任何跳转，只能手动刷新——用户会把这理解为"平台坏了"。修复后会话失效即安静地回到登录页，
重新登录后继续。这是"会话有有效期"这个安全设计和"用户无感知"之间必须补上的一环。

### 决策过程
- 备选 A：client.ts 里 401 时直接 `window.location.href = "/login"`。❌ 否决——API 层做导航是职责越界，且会丢失当前页面上下文。
- 备选 B：引入全局状态管理库。❌ 否决——为一个场景引入新依赖不值。
- ✅ 最终：CustomEvent 广播。API 层只负责"广播事实"，谁关心谁订阅，和现有 Context 架构兼容，无新依赖。
- 顺带修了 `login/register` 的状态一致性：以前 token 先持久化、`me()` 后确认，`me()` 失败会出现"页面报错但 token 已存"的中间态。

---

## 变更 2：职业画像「求职意向」永不显示（拼写错误）

### 技术变更
| 文件 | 变更 |
|---|---|
| `frontend/src/components/ProfileView.tsx:37` | `extras.care_goal` → `extras.career_goal`（检查的字段名和读取的字段名不一致，恒为 undefined） |

### 产品视角
「求职意向」是访谈 D6 维度的产出、画像页 hero 区的展示项——**用户花了整个访谈流程提供的核心信息，从来没展示过**。这类 bug 的危害在于完全静默：没有任何报错，只有"用户提供了但看不到"。一行 typo，影响的是访谈产品的价值闭环（我说的话，产品真的记住了吗？）。

---

## 变更 3：目标岗位「假编辑器」（matched 阶段编辑被静默丢弃）

### 技术变更
| 文件 | 变更 |
|---|---|
| `frontend/src/pages/TargetJob/index.tsx` | 编辑卡（JD 文本域 + 能力模型编辑器 + 确认按钮）只在 `stage === "editing"` 渲染；此前 `isParsed` 让 matched 阶段也渲染编辑卡，但 `draft` 为 null 导致 `onChange` 写不进状态、`handleConfirm` 直接 return |
| 同上 | `JobWorkspace` 新增 `onEdit` prop：hero 区提供「编辑 JD / 能力模型」入口，点击进入编辑态（以当前岗位为草稿，改动走同一条确认保存链路）；同时新增「重新匹配」按钮（模型改过之后旧匹配分会失真） |

### 产品视角
这个产品反复向用户传递"AI 只做草稿，**你有最终确认权**"的理念（DraftConfirm、能力模型手动编辑、确认后 AI 不覆盖）。假编辑器恰恰击穿了这个承诺的核心场景：用户看完匹配结果想微调岗位要求，输入框能打字但保存无反应。修复后的交互模型是干净的三段式：
**工作台（看结果）→ 显式点「编辑」→ 编辑态（改 + 确认）→ 回工作台**。
配套的「重新匹配」按钮是因为编辑能力模型后，旧的匹配分已经不反映当前模型——让"数据变了"这件事对用户可见、可操作，而不是默默展示过期分数。

### 决策过程
- 备选 A：让 matched 阶段的编辑卡直接可用（onChange 回退到 current）。❌ 否决——保留"编辑卡和工作台同时显示"的混乱布局，页面同时有两套"当前模型"的展示，认知负担大。
- ✅ 最终：编辑卡只属于编辑态；工作台提供显式编辑入口。产品语义从"到处都能改"收敛为"想改就进编辑态"，与 Onboarding/Profile 的编辑模式一致。

---

## 变更 4：岗位切换 Drawer 无效

### 技术变更
| 文件 | 变更 |
|---|---|
| `backend/app/api/target_job.py` | 新增 `POST /target-job/{id}/set-current` 路由 |
| `backend/app/services/jd_service.py` | 新增 `set_current_target_job()`：触碰 `updated_at`（`current` 的判定依据就是"最近更新的岗位"，单一事实源，不引入 is_current 列） |
| `frontend/src/api/client.ts` | 新增 `api.setCurrentTargetJob()` |
| `frontend/src/pages/TargetJob/index.tsx` | `handleSwitch` 从"GET 后丢弃结果"改为"set-current → reload"，切换真正生效 |

### 产品视角
用户同时投多个岗位是常态（这正是产品要解决的场景），"岗位列表 + 切换"是核心导航。修复前切换按钮点了等于没点，用户只能删掉重建。修复后的语义诚实且可解释：**"当前岗位 = 你最近在操作的岗位"**——和人的工作方式一致，也和现有数据模型（按 updated_at 排序）完全一致。

### 决策过程
- 备选 A：加 `is_current` 布尔列 + 迁移。❌ 否决——引入第二事实源（is_current 和 updated_at 可能矛盾），需要新迁移和回填，为一个已有明确判定规则的概念增加状态。
- ✅ 最终：切换 = 触碰 updated_at。后端改动 ~15 行，语义单一，`set-current` 端点的 docstring 写明了这个取舍。
- 补了 2 个测试：切换语义（`test_set_current_switches_current_job`）+ 跨用户隔离（`test_set_current_user_isolated`，404 不泄露存在性）。

---

## 变更 5：Explore → TargetJob 方向关联「虚标」

### 技术变更
| 文件 | 变更 |
|---|---|
| `backend/app/services/jd_service.py` | `parse_jd()` 新增 `direction_id` 参数：仅在岗位尚未绑定方向时写入（不覆盖已有绑定） |
| `backend/app/api/target_job.py` | `ParseIn` 增加 `direction_id` 字段并透传 |
| `frontend/src/api/client.ts` + `TargetJob/index.tsx` | `parseTargetJobJD` 携带 `direction_id`；方向横幅按真实绑定状态显示"已关联本岗位 / 解析 JD 后将关联到岗位" |

### 产品视角
"从职业探索选中一个方向 → 带着它去目标岗位"是这个产品的**关键认知链路**（探索是模糊的意愿，岗位是具体的决策）。横幅的作用是告诉用户"你没有丢失上下文"。但之前这个关联从未真正写进数据库——横幅是谎言，刷新后关联还会丢失。修复后关联在 parse 时落库，横幅文案与数据库事实一致。测试 `test_parse_binds_direction_once` 同时验证了"绑定"和"不覆盖用户已有绑定"两个语义。

---

## 变更 6：Onboarding「稍后再说」死循环

### 技术变更
| 文件 | 变更 |
|---|---|
| `frontend/src/pages/Onboarding/index.tsx` | `handleExit` 统一去 `/profile`（未定稿时 toast 提示"访谈进度已保存"）；不再去 `/login`（登录态下 `/login` 会重定向回 `/onboarding`，把用户弹回原点） |
| `frontend/src/components/ProfileHome.tsx` | 新增可选 `onResumeInterview` prop：渲染「继续 AI 访谈」通栏入口 |
| `frontend/src/pages/Profile/index.tsx` | 画像录入视图传入 `onResumeInterview={() => navigate("/onboarding")}`；顺带把两处 `children={...}` props 反模式改为 JSX 子元素写法 |
| `frontend/src/styles/pages.css` | 新增 `.entry-card--wide` 通栏样式 |

### 产品视角
"稍后再说"是**尊重用户节奏**的设计——访谈不该是强制流程。但原实现让这个出口变成了死循环：想离开的用户被弹回原地，六个工作区没有任何入口。修复后的闭环是：
**访谈 →「稍后再说」→ 画像工作区（可自己录入，也可一键回到访谈续聊）**。
访谈进度保存在服务端，"随时可回来"是真实的而不是安慰性文案。这一来"AI 访谈"和"表单录入"两条路径真正打通：用户可以混合使用两种方式完成画像。

### 决策过程
- 备选 A：未定稿时允许进工作区（Explore 等）。✅ 部分采纳——工作区本来就有优雅降级（Explore 显示"请先建立画像"引导页），去 /profile 天然可行。
- 备选 B：退出时登出。❌ 否决——"稍后再说"的语义是暂停不是放弃，登出过于惩罚性。
- 「继续访谈」入口放哪？✅ ProfileHome 录入视图（未定稿用户必经之地），与三种录入方式并列，视觉上是第四个入口而不是隐藏链接。

---

## 变更 7：自研迁移加事务保护

### 技术变更
| 文件 | 变更 |
|---|---|
| `backend/app/db/migrate.py` | 每个迁移的 `upgrade()` + 版本号写入包进**同一个事务**（`eng.begin()`）；runner 契约从"传入 Engine"改为"传入 Connection" |
| `backend/app/db/migrations/0006_prepare_snapshot.py`、`0010_prep_plan_ai_status.py` | 从 `eng.connect()` 自管事务改为直接在传入的 Connection 上执行 DDL |
| `backend/app/db/migrations/0008_offer.py`（间接） | 其调用的 `seed_city_costs()` 兼容 Engine/Connection 两种入参（`app/db/seed_city_cost.py`） |

### 产品视角
迁移失败原本有两层静默兜底（P0 已去掉 entrypoint 和 lifespan 的吞错），但迁移自身的执行仍是"先改表、后记版本"两步分离：改表成功但记版本失败 → 下次重复执行（靠各迁移自己的幂等检查兜底）；改表失败 → 半套 schema 留在库里。事务化后**要么完整生效并记版本，要么整体回滚**。对产品的意义：部署升级是用户无感的后台动作，它的可靠性应该达到"凌晨三点自动跑也不用有人盯着"的程度。

### 决策过程
- 备选 A：换 Alembic。❌ 否决——P1 不做框架迁移，10 个存量迁移的兼容成本高；自研 runner 只有 90 行，事务化后可靠性已够，Alembic 可作为远期选项。
- 备选 B：只加 try/except 回滚。❌ 否决——SQLAlchemy 的 `begin()` 上下文自带回滚语义，手动 try/except 是重复造轮子。
- 契约变更的影响面：逐个核查了 10 个迁移模块，只有 0006/0010 直接用 Engine API（已改），其余走 `create_all(conn)`/`inspect(conn)` 天然兼容 Connection。**conftest 每次测试都从零建库 = 全量重放 10 个迁移，94 个用例通过即验证了新契约**。

---

## 测试与验证

| 项 | 结果 |
|---|---|
| 后端 `pytest tests`（新增 3 用例：set-current 切换、set-current 隔离、方向绑定） | **94 passed** |
| 迁移事务契约（conftest 每用例从零重放 0001→0010） | 通过 |
| 前端 `npm run build`（tsc 编译 + vite 打包） | 通过 |
| 前端 `npm run typecheck` | ❌ 预存在脚本缺陷（`tsc -b --noEmit` 冲突），非本次引入，待 P2 |

## 遗留事项（P2 及以后）

1. 前端工程化：ESLint + Prettier + Vitest 落地（代码里已有 4 处 eslint-disable 注释在等它）；修 typecheck 脚本。
2. Offer 页 / Explore 页的错误处理盲区（无 catch 的 promise、无防重复提交）。
4. Profile / Onboarding 四处复制的 1 秒无限轮询 → 抽公共 hook + 轮询上限。
5. JWT 增加 refresh token / 过期感知（当前 7 天硬过期，配合变更 1 的 401 处理体验已可接受）。
6. 简历解析轮询改为后端推送（SSE/轮询带指数退避）。

## 给下一个 agent 的上下文

- 「当前岗位」的定义：`updated_at` 最新的岗位（`get_latest_target_job`），切换 = set-current 触碰时间戳。若未来要改成显式 is_current 列，需同步改 `jd_service.list_target_jobs` / `set_current_target_job` / home 前端逻辑。
- 迁移 runner 契约：`upgrade(conn)` 接收 Connection，整个迁移 + 版本写入在一个事务里。新增迁移时不要在内部调用 `eng.connect()`。
- 前端 401 的约定：client.ts 只在"带 token 的请求收到 401"时广播 `cdp:auth-expired`；AuthContext 订阅它清状态。新增请求封装代码时保留这个行为。
- Onboarding 会话的续聊入口在 `ProfileHome`（`onResumeInterview` prop），后端 `getOnboardingSession` 会自动恢复进度。
