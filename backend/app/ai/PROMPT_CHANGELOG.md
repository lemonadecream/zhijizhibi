# Prompt 版本日志（Changelog）

> 约定：每次修改 `app/ai/prompts.py` 追加一条；**改完必须跑评测**并把分数记在本页。
> 评测命令：`python -m evals.run_evals`（真实 key）+ `pytest tests/test_eval_golden.py`（mock 契约）。

## v3（2026-09-07 夜间）—— 职业理解 + 行业准确性 + 注入防护

| 任务 | 改动 | 目的 |
|---|---|---|
| F1 | 注入防御（数据非指令）；中文简历常识；compact few-shot | 防注入；提升中文简历抽取常识性 |
| F2 | 职能×行业定位要求（禁空话）；能力等级 1-5 市场标尺 | 职业理解度 |
| F9 | 注入防御；能力必须来自原文；compact few-shot | 防注入；防凑数 |
| F10 | 校准标准：covered 需直接证据，宁可保守 | 匹配可信度 |
| F4 | 行业表述限职业常识，不引用不可确认数字/事件 | 行业信息准确性 |
| F21 | 公开信号只做定性引用，禁止编造数字，不确定降 medium | 行业信息准确性 |
| 访谈 | 三轴澄清法（职能×行业×阶段）+ 具体选项降回答成本 | 职业理解度 |

- mock 契约评测：6/6 通过
- 真实模型基线：**待跑**（等用户配新 key）→ 分数记录处：____________

## v2（2026-09-06 之前的历史版本）

- v1：初版 12 任务 prompt（"AI 只解释、程序管数字"纪律、证据链要求）
- v2：访谈引擎六维度状态机、F10 关系枚举、F21 档位制、F24 注入程序数字
- 历史版本无逐条评测分数（评估闭环 v3 之后才建立）——这正是评估体系存在的理由。

## 模板（以后照抄）

```
## vN（日期）—— 主题
| 任务 | 改动 | 目的 |
- mock 契约评测：x/x 通过
- 真实模型评测：通过率 __%（golden_cases.json 用例数 __）
- 结论：采纳 / 回滚（原因）
```
